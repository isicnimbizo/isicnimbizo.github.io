---
label: People
hide_body: true
fields:
- type: field_group_list
  name: item
  label: item
  fields:
  - type: text
    name: name
    label: name
    config:
      required: true
  - type: list
    name: years
    label: years
    config:
      use_select: false
      source:
        type: simple
      min: 1
    description: Which year(s) did this person teach?<br>One year per line/entry/item
  - type: text
    name: affiliation
    label: affiliation
  - type: text
    name: link
    label: link
    config:
      required: true
  - name: image
    type: file
    config:
      maxSize: 64
    label: image
pages:
- data/faculty.yml
- data/faculty.yml
- data/tutors.yml
